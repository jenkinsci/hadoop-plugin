TODO:

 - verify the datanode hostname by checking if the master can connect to that IP.
   if that fails, revert to IP.
 - deploy hadoop binaries to slaves automatically 
 - figure out how to configure disk space consumption